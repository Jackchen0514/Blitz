# v1.0.14

## Fixes

- **Connection Limiter**: Fix regex not matching Hysteria2 log lines
  The event string ("client connected"/"client disconnected") appears
  unquoted in Hysteria2 console-format logs. Previous regex required
  quotes around the event, so no connections were ever counted and
  the limit was never enforced.
